// Write a function that takes two numbers and returns the maximum of the two:

let number = max(1, 2);
console.log(number);
function max(number1, number2){
    if(number1 > number2) return a;
    return b;

// It can also be written like this: return (a > b) ? a : b; which is the equivalent to the 2 lines above
}
